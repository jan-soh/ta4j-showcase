package de.jansoh.rsistrategy.model;

/**
 * Represents the side of an order in a trading system.
 * <p>
 * An order can either be a buy order (BUY) or a sell order (SELL).
 * This enumeration is typically used in financial or trading applications
 * to specify the intent or direction of a trade.
 */
public enum OrderSide {
    BUY,
    SELL;
}
